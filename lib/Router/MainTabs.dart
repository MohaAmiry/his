import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';

@RoutePage()
class CustomerHomeTabView extends StatelessWidget {
  const CustomerHomeTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CustomerHistoryTabView extends StatelessWidget {
  const CustomerHistoryTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CustomerProfileTabView extends StatelessWidget {
  const CustomerProfileTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CompanyHomeTabView extends StatelessWidget {
  const CompanyHomeTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CompanyAcceptedTabView extends StatelessWidget {
  const CompanyAcceptedTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CompanyDoneTabView extends StatelessWidget {
  const CompanyDoneTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class CompanyProfileTabView extends StatelessWidget {
  const CompanyProfileTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class AdminHomeTabView extends StatelessWidget {
  const AdminHomeTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class AdminDashboardTabView extends StatelessWidget {
  const AdminDashboardTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}

@RoutePage()
class AdminProfileTabView extends StatelessWidget {
  const AdminProfileTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return AutoRouter(builder: (context, content) => content);
  }
}
